//--------------------------------------------------------------------------------------
// Architechture
//--------------------------------------------------------------------------------------

Must create an asset manager class. 

//--------------------------------------------------------------------------------------
// Misc
//--------------------------------------------------------------------------------------

* The WINAPI file explorer in the menu bar does not properly add and render the imported OpenGL entity (improper context switching?). 
The same happens when trying to add a game object with mesh, at startup (in CWorld::Init()). The mesh is simply not being rendered.
The temporary solution is to add a delay. Must check why this problem happens without the delay.

* Remove the button(s) such as ImportButtonHWND and its/their functionality from case WM_COMMAND in LeftToolbarHwndProc, UIModule.cpp

* Timer bRunOnce wrong?

* Raypicking crashes